<?php

/**
 * Operadores aritméticos?
 * 
 * 
 * operadores:
 * Adição(+) - Soma valores
 * Subtração(-) - Diferença entre valores
 * Multiplicação(*) - Produto dos valores
 * Divisão(/) -8 Quociente dos valores
 * Módulo(%) - Resto existente em uma operação de divisão
 * Incremento(++$a) Pré incremento =>Adiciona uma unidade antes de retornar $a
 * Incremento($a++) pós incremento =>Retorna $a e depois adiciona uma unidade
 * decremento(--$a) Pré decremento =>Dimuini uma unidade antes de retornar $a
 * decremento($a--) pós decremento =>Retorna $a e depois diminui uma unidade
 * 
 */